{
  "name": "@uml-forge/tsconfig",
  "version": "0.0.0",
  "private": true,
  "description": "Presets de TypeScript compartidos por todo el monorepo",
  "files": [
    "base.json",
    "library.json",
    "react-library.json",
    "vite.json",
    "nest.json"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p test/base && tsc --noEmit -p test/library && tsc --noEmit -p test/nest",
    "test": "node --test test/presets.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
